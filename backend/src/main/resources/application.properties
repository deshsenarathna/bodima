server.address=0.0.0.0
server.port=9090

spring.application.name=demo
spring.datasource.url=jdbc:mysql://bodima-db.c1wa8008eb0x.ap-south-1.rds.amazonaws.com:3306/bodima_db
spring.datasource.username=admin
spring.datasource.password=Admin200142

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.web.resources.static-locations=file:uploads/
spring.mvc.static-path-pattern=/uploads/**

spring.servlet.multipart.max-file-size=8MB
spring.servlet.multipart.max-request-size=64MB

# Password reset email
spring.mail.host=${SMTP_HOST}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME}
spring.mail.password=${SMTP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${SMTP_HOST}

app.mail.from=${SMTP_FROM}
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:5173}

# CORS for frontend
spring.mvc.cors.mappings.[/**].allowed-origins=http://15.206.100.88
spring.mvc.cors.mappings.[/**].allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.mappings.[/**].allowed-headers=*
spring.mvc.cors.mappings.[/**].allow-credentials=true

