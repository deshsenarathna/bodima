---
- name: Deploy Bodima app
  hosts: web
  become: true
  vars:
    docker_users:
      - ec2-user
  pre_tasks:
    - name: Install AWS CLI
      ansible.builtin.yum:
        name: awscli
        state: present
  roles:
    - role: docker
    - role: app
